# Plaid (sandbox to start) — sign up free at https://dashboard.plaid.com
# Leave unset and the app runs fine with manual data only ("not connected" state).
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Required only once you connect a bank account. Generate with: npm run gen-key
ENCRYPTION_KEY=
